/** Placeholder — the real dependency graph lands with issue #10. */
export function GraphScreen() {
  return (
    <section style={{ padding: '16px 20px' }}>
      <h1 style={{ font: '600 17px/1.2 var(--font-ui)', letterSpacing: '-0.01em', margin: 0 }}>
        Dependency graph
      </h1>
      <p className="mono" style={{ color: 'var(--ink3)', fontSize: 11, marginTop: 8 }}>
        React Flow + ELKjs — issue #10
      </p>
    </section>
  )
}
